import sono1500st from './sono1500st.json';

const device = {
  id: "35209-09",
  name: "Расходомеры SONO 1500 ST.",
  hasModels: false,
  hasTypes: false,
  hasClasses: false,
  hasComposition: false,
  models: {
    sono1500st
  }
};

export default device;